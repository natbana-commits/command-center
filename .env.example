TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
CRON_SECRET=
GOOGLE_CALENDAR_ICS_URL=
ANTHROPIC_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
TELEGRAM_WEBHOOK_SECRET=

# Gmail + Drive + Tasks integration — optional, safe to leave blank (these
# features no-op until all three are set). One Google OAuth client covering
# gmail.readonly, drive.readonly, and tasks scopes. See docs/google-setup.md.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=

# Lecture transcription (Whisper) — optional, safe to leave blank (uploads
# just fail gracefully with an error until this is set). Needs a payment
# method on file at platform.openai.com.
OPENAI_API_KEY=
